import 'package:flutter/material.dart';


class CardsScree extends StatelessWidget {
  const CardsScree({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text("Cards Screen"),
      ),
      body: const Placeholder(),
    );
  }
}